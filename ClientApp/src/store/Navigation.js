"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// ----------------
// ACTION CREATORS - These are functions exposed to UI components that will trigger a state transition.
// They don't directly mutate state, but they can have external side-effects (such as loading data).
exports.actionCreators = {
    changeAuthenticated: function (authenticated) { return function (dispatch, getState) {
        // Only load data if it's something we don't already have (and are not already loading)
        var appState = getState();
        if (appState && appState.navigation && authenticated !== appState.navigation.authenticated) {
            dispatch({ type: 'CHANGE_AUTHENTICATED', authenticated: authenticated });
        }
    }; }
};
// ----------------
// REDUCER - For a given state and action, returns the new state. To support time travel, this must not mutate the old state.
var unloadedState = { authenticated: false };
exports.reducer = function (state, incomingAction) {
    if (state === undefined) {
        return unloadedState;
    }
    var action = incomingAction;
    switch (action.type) {
        case 'CHANGE_AUTHENTICATED':
            return {
                authenticated: action.authenticated
            };
        default:
            return state;
    }
};
//# sourceMappingURL=Navigation.js.map